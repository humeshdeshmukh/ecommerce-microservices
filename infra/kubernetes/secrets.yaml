apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-secrets
  namespace: ecommerce
type: Opaque
stringData:
  # IMPORTANT: DO NOT store production secrets in git.
  # These are placeholder keys. Replace via CI with real secrets or create K8s Secret from CLI.
  JWT_SECRET: "REPLACE_ME_JWT_SECRET"
  MONGO_ROOT_PASSWORD: "REPLACE_ME_MONGO_PW"
  POSTGRES_PASSWORD: "REPLACE_ME_POSTGRES_PW"
  REDIS_PASSWORD: "REPLACE_ME_REDIS_PW"
  # Add other secrets as needed
